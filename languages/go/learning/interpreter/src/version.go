package main

import "fmt"

const NAME = "MonkeyScript Interpreter"
const VERSION = "0.0.1"

// ----------------------------------------------------------------------------
func reportVersion() {
	fmt.Printf("%s v%s\n", NAME, VERSION)
}
