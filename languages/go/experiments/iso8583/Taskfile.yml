# https://taskfile.dev

version: "3"

vars:
  ROOT: src

tasks:
  default:
    cmds:
      - task --list-all
    silent: true
  run:
    cmds:
      - go run {{.ROOT}}/main.go
    silent: true
  test:
    cmds:
      - go test ./{{.ROOT}}/...
    silent: true
