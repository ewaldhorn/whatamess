# https://taskfile.dev

version: "3"

vars:
  GREETING: Hello, World!

tasks:
  default:
    desc: List all available tasks
    cmds:
      - task --list-all
    silent: true
  run:
    desc: Runs the webdev server
    cmds:
      - webdev serve
    silent: true
