# https://taskfile.dev

version: "3"

vars:
  PORT: 9090

tasks:
  default:
    desc: Lists available tasks
    cmds:
      - task --list-all
    silent: true
  run:
    desc: Runs the server on port {{.PORT}}
    cmds:
      - PORT={{.PORT}} dart run bin/server.dart
    silent: true
