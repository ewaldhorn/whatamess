let user = { name: "Bob", age: 30 };

console.log();
console.log("Hello, world!");
console.log(`User name: ${user.name} (${user.age}).`);

// ------------------------------------------------------------------------------------------------
let welcomeMessage: string;

welcomeMessage = "This is a welcome message";

console.log(`The message is "${welcomeMessage}".\n`);
