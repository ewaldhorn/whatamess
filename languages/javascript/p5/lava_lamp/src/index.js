import p5 from "../../lib/p5.js";

// ----------------------------------------------------------------------------
const lavaLamp = (p) => {
  // --------------------------------------------------------------------------
  p.setup = () => {
    p.createCanvas(800, 800);
    p.background(135, 206, 235); // Light blue background
  };

  // --------------------------------------------------------------------------
  p.draw = () => {
    p.background(135, 206, 235); // Light blue background
  };
};

// ----------------------------------------------------------------------------
new p5(lavaLamp, "p5-main-canvas");
