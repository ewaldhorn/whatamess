#include <stdio.h>

int main() {
    int a = 1, b = 2, c = 3;

    int sumOfAll = a + b + c;

    printf("The values are a=%d, b=%d, c=%d\n", a, b, c);
    printf("The sum total is %d\n", sumOfAll);

    return 0;
}